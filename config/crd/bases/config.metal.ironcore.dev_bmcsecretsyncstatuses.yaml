---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: bmcsecretsyncstatuses.config.metal.ironcore.dev
spec:
  group: config.metal.ironcore.dev
  names:
    kind: BMCSecretSyncStatus
    listKind: BMCSecretSyncStatusList
    plural: bmcsecretsyncstatuses
    singular: bmcsecretsyncstatus
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.bmcSecretRef
      name: BMCSecret
      type: string
    - jsonPath: .status.totalPaths
      name: Total
      type: integer
    - jsonPath: .status.successfulPaths
      name: Successful
      type: integer
    - jsonPath: .status.failedPaths
      name: Failed
      type: integer
    - jsonPath: .status.lastSyncAttempt
      name: Last Sync
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BMCSecretSyncStatus is the Schema for the bmcsecretsyncstatuses
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BMCSecretSyncStatusSpec defines the desired state of BMCSecretSyncStatus
            properties:
              bmcSecretRef:
                description: BMCSecretRef references the BMCSecret being tracked
                type: string
            required:
            - bmcSecretRef
            type: object
          status:
            description: BMCSecretSyncStatusStatus defines the observed state of BMCSecretSyncStatus
            properties:
              backendPaths:
                description: BackendPaths lists all backend paths where this secret
                  has been synced
                items:
                  description: BackendPath represents a single backend path that was
                    synced
                  properties:
                    bmcName:
                      description: BMCName is the name of the BMC resource associated
                        with this path
                      type: string
                    errorMessage:
                      description: ErrorMessage contains the error if sync failed
                      type: string
                    hostname:
                      description: Hostname is the hostname extracted from the BMC
                      type: string
                    lastSyncTime:
                      description: LastSyncTime is the timestamp when this path was
                        last synced
                      format: date-time
                      type: string
                    path:
                      description: Path is the full path in the backend where the
                        secret is stored
                      type: string
                    region:
                      description: Region is the region extracted from the BMC
                      type: string
                    syncStatus:
                      description: SyncStatus indicates if the sync was successful
                      enum:
                      - Success
                      - Failed
                      type: string
                    username:
                      description: Username is the username from the BMCSecret
                      type: string
                  required:
                  - bmcName
                  - hostname
                  - lastSyncTime
                  - path
                  - region
                  - syncStatus
                  - username
                  type: object
                type: array
              conditions:
                description: Conditions represent the latest available observations
                  of the sync status
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failedPaths:
                description: FailedPaths is the number of paths that failed to sync
                type: integer
              lastSyncAttempt:
                description: LastSyncAttempt is the timestamp of the last sync attempt
                format: date-time
                type: string
              successfulPaths:
                description: SuccessfulPaths is the number of paths successfully synced
                type: integer
              totalPaths:
                description: TotalPaths is the total number of paths that should be
                  synced
                type: integer
            required:
            - failedPaths
            - successfulPaths
            - totalPaths
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
