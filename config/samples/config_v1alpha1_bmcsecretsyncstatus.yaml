# This resource is automatically created and managed by the operator
# Users can view it to track BMCSecret synchronization status
#
# To view sync status for all secrets:
#   kubectl get bmcsecretsyncstatuses
#
# To view detailed status for a specific secret:
#   kubectl get bmcsecretsyncstatus <secret-name>-sync-status -o yaml
#
# Example output after syncing a BMCSecret named "admin-credentials":
apiVersion: config.metal.ironcore.dev/v1alpha1
kind: BMCSecretSyncStatus
metadata:
  name: admin-credentials-sync-status
spec:
  bmcSecretRef: admin-credentials
status:
  totalPaths: 2
  successfulPaths: 2
  failedPaths: 0
  lastSyncAttempt: "2026-02-23T10:30:00Z"
  backendPaths:
  - path: bmc/us-east-1/bmc1.example.com/admin
    bmcName: test-bmc-1
    region: us-east-1
    hostname: bmc1.example.com
    username: admin
    lastSyncTime: "2026-02-23T10:30:00Z"
    syncStatus: Success
  - path: bmc/us-west-1/bmc2.example.com/admin
    bmcName: test-bmc-2
    region: us-west-1
    hostname: bmc2.example.com
    username: admin
    lastSyncTime: "2026-02-23T10:30:00Z"
    syncStatus: Success
  conditions:
  - type: Synced
    status: "True"
    observedGeneration: 1
    lastTransitionTime: "2026-02-23T10:30:00Z"
    reason: AllPathsSynced
    message: Successfully synced to 2 backend paths
